services:
  mysql:
    image: 'mysql:latest'
    environment:
      - 'MYSQL_DATABASE=house'
      - 'MYSQL_PASSWORD=root'
      - 'MYSQL_ROOT_PASSWORD=root'
      - 'MYSQL_USER=spring_root'
    ports:
      - '3306'
